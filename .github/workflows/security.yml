name: security

on:
  push:
    branches: [main, '*.x']
  pull_request:
  schedule:
    - cron: '0 6 * * 1'

permissions:
  contents: read

env:
  APP_KEY: zatrano-dev-key-do-not-use-prod!
  APP_ENV: testing
  APP_DEBUG: 'true'

jobs:
  vet:
    name: go-vet
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages
    steps:
      - uses: actions/checkout@v5
        with:
          path: packages
      - uses: actions/checkout@v5
        with:
          repository: zatrano/framework
          ref: main
          path: framework
      - uses: actions/setup-go@v6
        with:
          go-version: '1.25.13'
          cache: true
          cache-dependency-path: packages/go.sum
      - run: go mod download
      - run: go vet ./...

  race:
    name: go-test-race
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages
    steps:
      - uses: actions/checkout@v5
        with:
          path: packages
      - uses: actions/checkout@v5
        with:
          repository: zatrano/framework
          ref: main
          path: framework
      - uses: actions/setup-go@v6
        with:
          go-version: '1.25.13'
          cache: true
          cache-dependency-path: packages/go.sum
      - run: go mod download
      - name: go test -race
        run: go test -race ./... -count=1

  gosec:
    name: gosec
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v5
        with:
          path: packages
      - uses: actions/checkout@v5
        with:
          repository: zatrano/framework
          ref: main
          path: framework
      - uses: actions/setup-go@v6
        with:
          go-version: '1.25.13'
          cache: true
          cache-dependency-path: packages/go.sum
      - name: Read gosec excludes from .github/gosec.json
        working-directory: packages
        id: gosec_cfg
        run: |
          EXCLUDES=$(jq -r '.exclude // [] | join(",")' .github/gosec.json)
          echo "exclude=$EXCLUDES" >> "$GITHUB_OUTPUT"
      - name: Run gosec
        uses: securego/gosec@master
        with:
          args: >
            -no-fail
            -fmt=sarif
            -out=gosec.sarif
            -conf=packages/.github/gosec.json
            -exclude=${{ steps.gosec_cfg.outputs.exclude }}
            -exclude-dir=packages/database/driver
            -exclude-dir=packages/qr
            -exclude-dir=packages/mongo
            -exclude-dir=packages/webauthn
            -exclude-generated
            packages/...
      - name: Upload gosec SARIF
        if: always()
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: gosec.sarif
          category: gosec
      - name: gosec gate (fail on findings)
        working-directory: packages
        run: |
          set -euo pipefail
          install_gosec() {
            go install github.com/securego/gosec/v2/cmd/gosec@v2.22.2
          }
          ok=0
          for i in 1 2 3 4 5; do
            if install_gosec; then
              ok=1
              break
            fi
            echo "gosec install failed (attempt $i); retrying..."
            sleep $((i * 8))
          done
          if [ "$ok" -ne 1 ]; then
            echo "gosec install failed after retries"
            exit 1
          fi
          EXCLUDES=$(jq -r '.exclude // [] | join(",")' .github/gosec.json)
          gosec -quiet \
            -conf=.github/gosec.json \
            -exclude="$EXCLUDES" \
            -exclude-dir=database/driver \
            -exclude-dir=qr \
            -exclude-dir=mongo \
            -exclude-dir=webauthn \
            -exclude-generated \
            ./...

  govulncheck:
    name: govulncheck
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages
    steps:
      - uses: actions/checkout@v5
        with:
          path: packages
      - uses: actions/checkout@v5
        with:
          repository: zatrano/framework
          ref: main
          path: framework
      - uses: actions/setup-go@v6
        with:
          go-version: '1.25.13'
          cache: true
          cache-dependency-path: packages/go.sum
      - run: go mod download
      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@v1.1.4
      - name: Run govulncheck
        shell: bash
        run: |
          set +e
          govulncheck ./... | tee govulncheck.txt
          code=${PIPESTATUS[0]}
          {
            echo "## govulncheck"
            echo ""
            echo '```'
            cat govulncheck.txt
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
          exit $code

  semgrep:
    name: semgrep
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep
    steps:
      - uses: actions/checkout@v4
      - name: Semgrep community (report only)
        run: |
          semgrep scan \
            --config=p/golang \
            --config=p/security-audit \
            --metrics=off \
            . || true
      - name: Semgrep ZATRANO rules (fail on findings)
        run: |
          semgrep scan \
            --config=.github/semgrep/zatrano-rules.yml \
            --severity=ERROR \
            --error \
            --metrics=off \
            .

  trivy:
    name: trivy-fs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Trivy FS (report all)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          scanners: vuln,secret,misconfig
          format: table
          exit-code: '0'
      - name: Trivy FS (fail on HIGH/CRITICAL)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          scanners: vuln,secret,misconfig
          severity: HIGH,CRITICAL
          exit-code: '1'

  fuzz:
    name: fuzz
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages
    strategy:
      fail-fast: false
      matrix:
        target:
          - FuzzSanitizeIdentifier
    steps:
      - uses: actions/checkout@v5
        with:
          path: packages
      - uses: actions/checkout@v5
        with:
          repository: zatrano/framework
          ref: main
          path: framework
      - uses: actions/setup-go@v6
        with:
          go-version: '1.25.13'
          cache: true
          cache-dependency-path: packages/go.sum
      - run: go mod download
      - name: Fuzz ${{ matrix.target }} (3m)
        run: bash .github/scripts/run-fuzz.sh ${{ matrix.target }} ./database/query/ 3m
      - name: Upload fuzz crash corpus
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: fuzz-crash-${{ matrix.target }}
          path: packages/database/query/testdata/fuzz/
          if-no-files-found: ignore

  summary:
    name: Security Summary
    runs-on: ubuntu-latest
    if: always()
    needs: [vet, race, gosec, govulncheck, semgrep, trivy, fuzz]
    steps:
      - name: Write summary table
        run: |
          status() {
            case "$1" in
              success) echo PASS ;;
              failure) echo FAIL ;;
              cancelled) echo CANCELLED ;;
              skipped) echo SKIPPED ;;
              *) echo WARNING ;;
            esac
          }
          {
            echo "## Security Summary"
            echo ""
            echo "| Tool | Result |"
            echo "|------|--------|"
            echo "| go vet | $(status '${{ needs.vet.result }}') |"
            echo "| go test -race | $(status '${{ needs.race.result }}') |"
            echo "| gosec | $(status '${{ needs.gosec.result }}') |"
            echo "| govulncheck | $(status '${{ needs.govulncheck.result }}') |"
            echo "| Semgrep | $(status '${{ needs.semgrep.result }}') |"
            echo "| Trivy FS | $(status '${{ needs.trivy.result }}') |"
            echo "| Fuzz | $(status '${{ needs.fuzz.result }}') |"
          } >> "$GITHUB_STEP_SUMMARY"
          for r in ${{ needs.vet.result }} ${{ needs.race.result }} ${{ needs.gosec.result }} ${{ needs.govulncheck.result }} ${{ needs.semgrep.result }} ${{ needs.trivy.result }} ${{ needs.fuzz.result }}; do
            if [ "$r" = "failure" ]; then
              exit 1
            fi
          done
