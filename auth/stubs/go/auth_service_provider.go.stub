package providers

import (
	"github.com/zatrano/framework/v2/contracts"
)

// AuthServiceProvider registers authorization gates and policies.
type AuthServiceProvider struct{}

func (p *AuthServiceProvider) Register(app contracts.App) error {
	return nil
}

func (p *AuthServiceProvider) Boot(app contracts.App) error {
	// Register gates and policies here, e.g.:
	// authorization.From(app).Policy("user", policies.NewUserPolicy())
	return nil
}
